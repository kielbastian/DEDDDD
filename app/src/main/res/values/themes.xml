<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Motyw bazowy dla Compose for TV — bez ActionBara, tło czarne (OLED/TV). -->
    <style name="Theme.MiBoxIptv" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
